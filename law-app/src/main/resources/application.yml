spring:
  application:
    name: io.law
  
  main:
    web-application-type: none
  
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/law_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:root}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      keepalive-time: 300000
      connection-test-query: SELECT 1
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 50  # Batch INSERT de 50 lignes à la fois
        order_inserts: true  # Optimisation : groupe les INSERTs par entité
        order_updates: true  # Optimisation : groupe les UPDATEs par entité
    open-in-view: false
  
  batch:
    job:
      enabled: false  # Disable auto-run on startup
    jdbc:
      initialize-schema: always

server:
  port: 8080

# Application specific configuration
law:
  base-url: https://sgg.gouv.bj/doc
  user-agent: Mozilla/5.0 (compatible; LawBatchBot/1.0)
  end-year: 1960
  max-number-per-year: 2000
  
  storage:
    base-path: data
    pdf-dir: pdfs
    ocr-dir: ocr
    json-dir: articles
  
  http:
    timeout: 30000
  
  ocr:
    language: fra
    dpi: 300
    quality-threshold: 0.70
  
  batch:
    chunk-size: 2000
    max-threads: 20  # 1 thread par vCPU (KVM 8 = 8 vCPU)
    max-items-to-fetch-previous: 8000  # 2000/an × 2 types × 2 années = avance de 2 ans par cycle
  
  capacity:
    ia: 4   # Activé pour Ollama gemma3n:latest (KVM 8 : 32GB RAM, 8 vCPU)
    ocr: 2  # Score OCR (4GB+)
    ollama-url: http://localhost:11434
    ollama-models-required: gemma3n:latest

  groq:
    api-key: ${GROQ_API_KEY:}

quality:
  sequence-penalty: enabled   # Pénalité si numérotation des articles non séquentielle
  dictionary-penalty: enabled # Pénalité progressive via mots non reconnus
  unrecognized-words-file: data/word_non_recognize.txt

logging:
  level:
    root: INFO
    bj.gouv.sgg: DEBUG
    org.springframework.batch: INFO
    org.springframework.batch.core.configuration.annotation.BatchObservabilityBeanPostProcessor: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
